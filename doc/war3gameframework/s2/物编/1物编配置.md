## 物编配置
- `config_loader.lua`
1. 基础元表
2. 物编配置
3. 技能实现为运行时代码

## 基础元表
```lua
yo.ini.mt_unit = {}
yo.ini.mt_ability = {}
yo.ini.mt_item = {}
```

## 物品配置
```lua
yo.ini.unit = {}
yo.ini.ability = {}
yo.ini.item = {}
yo.ini.destruct = {}
yo.ini.state={}

require "config.unit.敌机.熊王.init"
require "config.unit.自机.魂魄妖梦.幽人的庭师.init"
```
- 单位AI物编
```lua
-- 自定义物编 覆盖yo.ini.mt_unit
yo.ini.unit["熊王"] = setmetatable({
    obj = {},
    ini = {},
    -- AI名字(表结构)
    -- 随机AI     {{{"", "", ""},0.2},{"", "", ""},0.4},{"", "", ""},0.4},}
    random_states = nil,
    -- 周期AI   {{"",1400},{"",1500},{"",3000},}
    period_states = {{"熊王-天崩地裂",100},{"熊王-给你一巴掌",5}},
    -- 事件AI   {"", "", ""}
    event_states = {"冰熊-莫挨老子"}

},yo.ini.mt_unit)
```
2. AI导入
```lua
require "config.unit.敌机.熊王.熊王-天崩地裂"
require "config.unit.敌机.熊王.熊王-给你一巴掌"
require "config.unit.敌机.熊王.冰熊-莫挨老子"
```
3. 熊王-天崩地裂
- AI运行时的代码实现
```lua
yo.ini.state["熊王-天崩地裂"] = function(_ai)
    local _state = yo.state.new(_ai)
    local _unit = _ai._owner

    local charge = yo.state.new_aciton()
    local jump = yo.state.new_aciton()
    local hit_fly = yo.state.new_aciton()

    -- 定义流程(充能完毕后执行跳跃)
    function charge:on_finish() jump:on_action() end
    -- 定义流程(跳跃完毕后执行击飞)
    function jump:on_finish() hit_fly:on_action() end

     -- 定义流程(击飞完毕后再次击飞,重复执行两次)
    local loop = 0
    function hit_fly:on_finish()
        loop = loop + 1
        if loop > 1 then
            _state:exit()
            loop = 0
        else
            hit_fly:on_action()
        end
    end

    -- 定义流程(此状态的入口状态为充能状态)
    function _state:on_enter()
        charge:on_action()
    end
end
```
- 单位技能物编
```lua
yo.ini.unit["幽人的庭师"] = setmetatable({
    obj = {},
    ini = {},

    -- 技能名字
    ability_names = {"", "", "", "", ""},

},yo.ini.mt_unit)
```
- 导入单位物编
```lua
require "config.unit.自机.魂魄妖梦.幽人的庭师.折叠「火同眼剑」"
require "config.unit.自机.魂魄妖梦.幽人的庭师.断命剑「冥想斩」"
require "config.unit.自机.魂魄妖梦.幽人的庭师.天赋被动"
```
- 技能物编 运行时
```lua
yo.ini.ability["断命剑「冥想斩」"] = function(str_id)
    local mt = yo.ability.new(str_id)
    return mt
end
```

- 物品物编
```lua
yo.ini.item["兽王之鼓"] = function()
end
```